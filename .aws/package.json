{
  "name": "prospect-curation-api",
  "version": "1.0.0",
  "main": "dist/src/main.js",
  "types": "src/main.ts",
  "license": "MPL-2.0",
  "private": true,
  "scripts": {
    "get": "cdktf get",
    "build:dev": "npm run build && NODE_ENV=development npm run synth",
    "build": "rm -rf dist && tsc",
    "synth": "cdktf synth",
    "compile": "tsc --pretty",
    "watch": "tsc -w",
    "test": "echo ok",
    "upgrade": "npm i cdktf@latest cdktf-cli@latest",
    "upgrade:next": "npm i cdktf@next cdktf-cli@next"
  },
  "engines": {
    "node": ">10"
  },
  "dependencies": {
    "@cdktf/provider-aws": "^2.0.9",
    "@cdktf/provider-pagerduty": "^0.0.3",
    "@pocket-tools/terraform-modules": "1.24.11",
    "constructs": "^3.3.125"
  },
  "devDependencies": {
    "@types/node": "^16.6.1",
    "typescript": "4.3.5"
  }
}
